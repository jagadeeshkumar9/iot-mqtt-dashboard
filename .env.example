# Example environment variables for docker-compose (do NOT commit secrets to VCS)

# MQTT credentials used by the bridge to connect to the broker (optional)
MQTT_USER=
MQTT_PASS=

# Dashboard basic auth credentials (protects the dashboard and websocket handshake)
DASH_USER=admin
DASH_PASS=changeme
